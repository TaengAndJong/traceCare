<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 네트워크 (Backend API 호출) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 위치 — Coding_Convention.md/API_Specification.md §4.1 위치 전송 기능.
         ACCESS_FINE_LOCATION만으로는 화면이 꺼지거나 앱이 백그라운드로 가면 Android 8+ 정책상 위치 수집이 제한되므로
         ACCESS_BACKGROUND_LOCATION을 함께 요청한다(런타임에서는 2단계로 나누어 요청 — LocationPermissionScreen 참고). -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- Foreground Service — flutter_foreground_task가 백그라운드에서도 주기적으로 위치를 전송하기 위해 필요.
         Android 14(API 34)부터 Foreground Service Type을 명시하지 않으면 거부되므로 location 타입을 선언한다. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <!-- Foreground Service는 지속 알림이 필수(Android 정책) — Android 13+는 알림 권한을 별도로 요구한다. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:label="tracecare_app"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- flutter_foreground_task 기본 서비스 선언을 location 타입으로 오버라이드한다.
             (runtime의 FlutterForegroundTask.startService(serviceTypes: [ForegroundServiceTypes.location])와
             일치시켜야 Android 14+에서 타입 불일치로 거부되지 않는다.) -->
        <service
            android:name="com.pravera.flutter_foreground_task.service.ForegroundService"
            android:foregroundServiceType="location"
            android:exported="false"
            tools:replace="android:foregroundServiceType" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
